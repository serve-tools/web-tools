{
	"name": "@serve-tools/server-realtime",
	"version": "0.1.1",
	"description": "Sans-I/O typed request and subscription server core",
	"keywords": [
		"realtime",
		"rpc",
		"sans-io",
		"server",
		"subscription",
		"typescript"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/server/realtime#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "server/realtime"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/server-realtime.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"build": "tsc --build tsconfig.json",
		"build:dependencies": "npm run build --workspace @serve-tools/polyfill-report-error --workspace @serve-tools/realtime-protocol",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/polyfill-report-error": "^0.1",
		"@serve-tools/realtime-protocol": "^0.1"
	}
}
