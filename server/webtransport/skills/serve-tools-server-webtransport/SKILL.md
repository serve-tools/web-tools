---
name: serve-tools-server-webtransport
description: Use @serve-tools/server-webtransport for typed reliable handlers and typed best-effort datagrams on WebTransport servers.
---

# Use @serve-tools/server-webtransport

Treat the installed package README and public declarations as the API source of truth.
Read only the references needed for the task.

## Route by task

- [Recipe: custom WebTransport session](references/recipe-quick-start.md): compile-checked operation handlers, datagram handlers, context, and session transport.
- To choose the Node adapter or another runtime and preserve transport boundaries, read [Preserve session semantics](references/preserve-session-semantics.md).
