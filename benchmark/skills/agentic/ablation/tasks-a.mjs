const task = (id, family, title, prompt, packages, requiredRuntimeImports, requirements, mutants) => ({
	id,
	family,
	title,
	prompt: `Create a single solution.ts module that exports ${prompt}`,
	packages,
	requiredRuntimeImports,
	smokePath: `ablation/tasks/${id}/smoke.mjs`,
	hiddenPath: `ablation/tasks/${id}/hidden.mjs`,
	fixturePath: `ablation/tasks/${id}/solution.fixture.ts`,
	requirementMatrix: requirements.map(([id, description, critical = true]) => ({ id, description, critical })),
	mutants,
});

export const tasksA = [
	task(
		"with-cleanup-result",
		"resource-management",
		"Cleanup after one work result",
		"withCleanup<T>(work, cleanup): Promise<T>. Validate both arguments as functions before calling either; invalid input may throw synchronously or return a rejected promise with TypeError, and an invalid cleanup must not call work. Call and await work() once, then call and await cleanup() exactly once whether work fulfills or throws/rejects. If both fulfill, resolve the exact work value. If exactly one fails, reject with that exact failure value, including undefined, null, or 0. If both fail, reject with an instance of the package SuppressedError whose error is the exact cleanup failure and whose suppressed is the exact work failure. Use the package AsyncDisposableStack and package symbols; do not use native disposal symbols.",
		["@serve-tools/ponyfill-resource-management"],
		[["@serve-tools/ponyfill-resource-management"]],
		[
			["success", "cleanup follows successful work and the returned value is unchanged"],
			["failure", "work failure still cleans up and dual failure preserves identities"],
		],
		[
			{
				id: "skip-failed-cleanup",
				before: "await stack.disposeAsync();",
				after: "if (!workFailed) await stack.disposeAsync();",
				requirementIds: ["failure"],
			},
			{
				id: "swallow-work-error",
				before: "if (workFailed) {\n\t\tthrow workError;\n\t}",
				after: "if (workFailed) {\n\t\treturn value!;\n\t}",
				requirementIds: ["failure"],
			},
		],
	),
	task(
		"moved-cleanup-bag",
		"resource-management",
		"Moved synchronous cleanup bag",
		"createCleanupBag(): { defer(callback): void; move(): CleanupBag; dispose(): void }. defer requires a function and otherwise throws TypeError without changing the bag. Active-owner callbacks run exactly once in LIFO order. move() uses the package DisposableStack.move() and returns a distinct new owner containing every callback registered so far; the old owner is immediately closed, so each later old-owner defer() or move() throws ReferenceError, while old-owner dispose() is a no-op. The new owner remains active and accepts more callbacks. dispose() is idempotent, and move() after disposal throws ReferenceError. Use the package DisposableStack.",
		["@serve-tools/ponyfill-resource-management"],
		[["@serve-tools/ponyfill-resource-management"]],
		[
			["transfer", "move transfers callbacks without double cleanup"],
			["lifecycle", "closed old owner and idempotent disposal"],
		],
		[
			{
				id: "no-transfer",
				before: "return new CleanupBag(this.stack.move());",
				after: "return new CleanupBag(this.stack);",
				requirementIds: ["transfer"],
			},
			{
				id: "ignore-invalid-defer",
				before: "defer(callback: () => void): void {\n\t\tthis.stack.defer(callback);\n\t}",
				after: 'defer(callback: () => void): void {\n\t\tif (typeof callback !== "function") return;\n\t\tthis.stack.defer(callback);\n\t}',
				requirementIds: ["lifecycle"],
			},
		],
	),
	task(
		"progress-fold",
		"async-operation",
		"Backpressured progress fold",
		"createProgressFold(values, initial, fold, options?): AsyncOperation<{ index: number; total: number }, number>. values must be an iterable whose materialized sequence is dense and contains only finite numbers; initial must be finite and fold callable. options defaults to {}, may contain unrelated extra properties, and supports highWaterMark (default 0, otherwise a non-negative finite integer) and signal (an AbortSignal when present). Return the started operation immediately. In input order, sequentially await fold(total, value, zero-based index, operationSignal), require each returned total to be finite, then await writing the exact fresh { index, total } progress record before invoking the next fold. The operation result is the final total, including initial for empty input. Manual abort(reason) must expose the canonical operation abort reason by identity on result, pass the aborted operationSignal to an in-flight fold, stop later folds, and keep finished pending until a signal-observing in-flight fold settles. An external options.signal has the same cancellation behavior. Invalid input may throw TypeError synchronously or reject the operation surfaces. Use AsyncOperation.",
		["@serve-tools/async-operation"],
		[["@serve-tools/async-operation"]],
		[
			["fold", "ordered accumulated observations and final total"],
			["backpressure", "writes are awaited"],
			["abort", "manual abort reaches fold and finished waits"],
		],
		[
			{
				id: "skip-total-two-write",
				before: "await write({ index, total });",
				after: "if (total === 2) void write({ index, total }); else await write({ index, total });",
				requirementIds: ["backpressure"],
			},
			{
				id: "accept-invalid-total",
				before: 'if (!Number.isFinite(total)) {\n\t\t\t\t\tthrow new TypeError("fold result");\n\t\t\t\t}',
				after: 'if (false) {\n\t\t\t\t\tthrow new TypeError("fold result");\n\t\t\t\t}',
				requirementIds: ["fold"],
			},
		],
	),
	task(
		"operation-branch-log",
		"async-operation",
		"Filtered operation branch",
		"collectEvenSquares(values, listener): Promise<{ all: number; evens: number[] }>. Before producing, validate values as an iterable whose materialized sequence is dense and contains only finite numbers, and validate listener as callable; invalid input throws or rejects with TypeError without invoking listener. Internally create one AsyncOperation that writes each original value in order and returns the total input count. Consume it through one AsyncOperationSubscriber branch that filters even values and maps them to squares. Await listener(square, zero-based branchIndex) sequentially in input order, and resolve an exact fresh object with all equal to the total input count and evens a fresh array of the squared even values. If listener throws or rejects with any value, reject with that exact value and stop producer work before any later input is written or observed. Use AsyncOperation and AsyncOperationSubscriber.",
		["@serve-tools/async-operation"],
		[["@serve-tools/async-operation"]],
		[
			["projection", "only even squares and branch-local indexes"],
			["callback-failure", "listener error remains the rejection reason"],
		],
		[
			{
				id: "skip-six",
				before: "value % 2 === 0",
				after: "value % 2 === 0 && value !== 6",
				requirementIds: ["projection"],
			},
			{
				id: "swallow-listener",
				before: "await listener(value, index);",
				after: "try { await listener(value, index); } catch {}",
				requirementIds: ["callback-failure"],
			},
		],
	),
	task(
		"reaction-threshold",
		"reactive-state",
		"Threshold notifier",
		"createThresholdNotifier(initial, threshold, publish): { set(value): void; snapshot(): { value: number; above: boolean }; dispose(): void }. initial and threshold must be finite numbers and publish callable; invalid inputs throw TypeError before publication. above is inclusive value >= threshold. Construction synchronously calls publish once with a fresh snapshot. Each effective Object.is-distinct finite set updates state immediately; one or more effective sets before the next microtask coalesce into exactly one publication of the latest fresh snapshot, while an Object.is-equal set publishes nothing. snapshot() always returns a fresh exact object. dispose() is idempotent, synchronously cancels a queued publication, prevents later publications, and leaves validated set() and snapshot() usable. Use Signal.State, Signal.Computed, and effect.",
		["@serve-tools/signal", "@serve-tools/signal-effect"],
		[["@serve-tools/signal"], ["@serve-tools/signal-effect"]],
		[
			["threshold", "inclusive threshold and fresh snapshots"],
			["timing", "initial sync and batched publication"],
			["dispose", "queued update is cancelled"],
		],
		[
			{
				id: "threshold-five",
				before: "value.get() >= threshold",
				after: "value.get() >= threshold && threshold !== 5",
				requirementIds: ["threshold"],
			},
			{ id: "no-dispose", before: "dispose: stop", after: "dispose: () => {}", requirementIds: ["dispose"] },
		],
	),
	task(
		"tag-window",
		"reactive-state",
		"Reactive unique tag window",
		"createTagWindow(initial, publish): { add(tag): boolean; delete(tag): boolean; setLimit(limit): void; snapshot(): { count: number; tags: string[] }; dispose(): void }. initial must be an iterable whose materialized sequence is dense and contains only non-empty strings, and publish must be callable; invalid construction throws TypeError before publication. Keep unique tags in first-insertion order, with duplicate initial entries ignored, and use a limit that defaults to 3 and accepts only non-negative safe integers. snapshot() returns a fresh exact object and fresh tags array: count is the total unique-tag count and tags is the first limit tags. add/delete require a non-empty string, return true only when membership changed, and preserve insertion order; setLimit changes only an Object.is-distinct valid limit. Construction publishes one fresh snapshot synchronously. Effective membership or limit changes coalesce to exactly one latest publication per microtask; duplicate add, missing delete, and same-limit calls publish nothing. dispose() is idempotent, cancels a queued publication, and prevents later publications. Use SignalSet, Signal.State, and effect.",
		["@serve-tools/signal", "@serve-tools/signal-collections", "@serve-tools/signal-effect"],
		[["@serve-tools/signal-collections"], ["@serve-tools/signal-effect"]],
		[
			["state", "unique insertion order and limit"],
			["timing", "no-op and batching behavior"],
			["dispose", "cancels queued publish"],
		],
		[
			{
				id: "move-duplicate",
				before: "if (tags.has(tag)) {\n\t\t\t\treturn false;\n\t\t\t}",
				after: "tags.delete(tag);",
				requirementIds: ["state"],
			},
			{
				id: "wrong-delete-result",
				before: "return tags.delete(tag);",
				after: "tags.delete(tag); return true;",
				requirementIds: ["timing"],
			},
		],
	),
	task(
		"delayed-echo-channel",
		"client-messaging",
		"Cancellable delayed echo channel",
		"openDelayedEcho(wait): Promise<{ echo(text, delay, signal?): Promise<string>; close(reason?): void; closed: Promise<void> }>. wait must be callable and receives (delay, requestSignal); invalid wait rejects open with TypeError. Own both ends of a MessageChannel, build the protocol with connect and serve, await the handshake before open resolves, and await wait before returning the same text. echo requires a non-empty string, a non-negative safe-integer delay, and an AbortSignal when signal is supplied. Invalid text/delay must reject through the remote request with TypeError without calling wait; an invalid signal must reject with TypeError. Aborting a caller signal while pending must promptly reject echo with the caller signal's exact reason and eventually abort the same requestSignal passed to wait; the server wait observes and settles that cancellation independently. close(reason?) is idempotent, closes both protocol owners and ports, causes every pending or later echo to reject rather than hang, and closed fulfills once both protocol sides close. Use connect and serve.",
		["@serve-tools/client-messaging"],
		[["@serve-tools/client-messaging"]],
		[
			["request", "handshake and delayed echo"],
			["validation", "remote invalid input has no wait"],
			["abort-close", "abort/close settle requests"],
		],
		[
			{
				id: "invalid-waits",
				before: "validate(input);\n\t\t\t\tawait wait",
				after: "await wait",
				requirementIds: ["validation"],
			},
			{
				id: "ignore-signal",
				before: "wait(input.delay, signal)",
				after: "wait(input.delay, new AbortController().signal)",
				requirementIds: ["abort-close"],
			},
		],
	),
	task(
		"bounded-event-channel",
		"client-messaging",
		"Scoped topic event channel",
		"openEventChannel(initial = []): Promise<{ append(topic, value): Promise<void>; list(topic): Promise<Array<{ topic: string; value: number }>>; watch(topic, listener): { unsubscribe(): void }; close(reason?): void; closed: Promise<void> }>. initial must be an iterable whose materialized sequence is dense; every item must be a non-null object with non-empty string topic and finite-number value, and unrelated item properties are ignored. Invalid initial rejects open with TypeError before channel ownership begins. Own both MessageChannel ends with connect and serve and await the handshake. append validates topic/value remotely, atomically stores an exact cloned { topic, value } record, and emits a fresh exact clone only to matching-topic watchers in append order; invalid append rejects with TypeError without state or events. list validates topic remotely and returns a fresh array of fresh exact records. watch validates a non-empty topic and callable listener, has no replay, and returns a handle whose idempotent unsubscribe removes its server subscription once and prevents later events. Subscription delivery may be asynchronous relative to append fulfillment but preserves append order. close(reason?) is idempotent, closes owners and ports, makes pending/later append, list, or watch activity settle rather than hang, and closed fulfills after both protocol sides close. Use connect and serve.",
		["@serve-tools/client-messaging"],
		[["@serve-tools/client-messaging"]],
		[
			["topics", "filtering and ordered delivery"],
			["isolation", "fresh stored/list values and invalid atomicity"],
			["ownership", "subscription cleanup and close"],
		],
		[
			{
				id: "broadcast",
				before: "if (watcher.topic === input.topic) {\n\t\t\t\t\t\twatcher.emit({ ...record });\n\t\t\t\t\t}",
				after: "if (true) {\n\t\t\t\t\t\twatcher.emit({ ...record });\n\t\t\t\t\t}",
				requirementIds: ["topics"],
			},
			{
				id: "truncate-value",
				before: "records.push(record);",
				after: "records.push({ ...record, value: Math.trunc(record.value) });",
				requirementIds: ["isolation"],
			},
		],
	),
	task(
		"download-link",
		"router",
		"Canonical download link",
		"buildDownloadLink(input): string. Define one typed route /exports/:reportId.:format with router route and codec. input must be a non-null, non-array closed object with exactly the required reportId and format fields plus optional columns and gzip; reject missing fields, unknown enumerable keys, and explicit null optionals with TypeError. reportId is a positive safe integer; format is exactly csv or json; columns defaults to [], otherwise it is a dense array of non-empty well-formed Unicode strings; gzip defaults to false and otherwise must be boolean. Return the canonical relative href: encode path/search values, preserve repeated-column order, omit absent/empty columns, omit gzip when false, and encode gzip=true when true. Reject sparse columns, lone-surrogate text, and every invalid path or search value with TypeError. Use the typed route's href generation rather than manual URL assembly.",
		["@serve-tools/router"],
		[["@serve-tools/router"]],
		[
			["href", "typed encoding, defaults, and repeated order"],
			["validation", "closed input and canonical positive identifier"],
		],
		[
			{
				id: "json-only-gzip",
				before: '...(input.gzip ? { gzip: "true" } : {})',
				after: '...(input.gzip && input.format === "json" ? { gzip: "true" } : {})',
				requirementIds: ["href"],
			},
			{
				id: "allow-zero-id",
				before: "input.reportId <= 0",
				after: "input.reportId < 0",
				requirementIds: ["validation"],
			},
		],
	),
	task(
		"request-target-inspector",
		"router",
		"Typed request-target inspection",
		'inspectRequestTarget(input): { channel: "stable" | "beta"; page: number | undefined; labels: string[] } | null. Define and match one typed /releases/:channel route: channel is stable or beta, page is one optional canonical integer scalar, and label is a repeatable string search field. Accept URL instances and absolute or relative strings (resolve relative strings against any internal HTTP base without exposing it). Return null, rather than throwing, for unsupported input types, malformed URL or percent encoding, non-HTTP(S) schemes, unmatched path/channel, invalid/noncanonical page, or repeated page values. On a match return a fresh exact object with decoded labels in query order and a fresh labels array; absent page is undefined and absent labels is []. Ignore unknown search keys, and ignore origin and fragment. Use route matching rather than manual parsing.',
		["@serve-tools/router"],
		[["@serve-tools/router"]],
		[
			["match", "decoded route/search and ignored unknowns"],
			["invalid", "null on malformed input and noncanonical scalar"],
		],
		[
			{
				id: "normalize-leading-zero",
				before: "const match = releases.match(url);",
				after: 'const match = releases.match(new URL(url.href.replace("page=01", "page=1")));',
				requirementIds: ["invalid"],
			},
			{
				id: "drop-hidden-label",
				before: "labels: [...match.search.label]",
				after: 'labels: match.search.label.filter((label) => label !== "hidden")',
				requirementIds: ["match"],
			},
		],
	),
	task(
		"etag-status-handler",
		"http-contract",
		"Revision-aware status handler",
		'createStatusHandler(initial = []): (request: Request) => Promise<Response>. Define a typed Fetch API for GET and PUT /statuses/:service, where service is api or worker. initial must be an iterable whose materialized sequence is dense; every record must be an exact non-null { service: "api" | "worker", status: "up" | "down", revision: positive safe integer }, otherwise construction throws TypeError. Clone initial records, and let the last duplicate service win. GET returns 200 with a fresh exact { service, status, revision } or 404 with exact { error: "not_found" }. PUT accepts an exact JSON object { status: "up" | "down", revision: positive safe integer }; when revision is strictly greater than the stored revision (or none exists), clone/store it and return 200 with the exact item. Equal or lower revision leaves state unchanged and returns 409 with exact { error: "stale_revision", revision: storedRevision }. Configure the server adapter with maxBodyBytes 64 and let its canonical responses handle malformed JSON, non-JSON media type, oversized bodies, schema failures, unmatched routes, and unsupported methods; do not catch or replace adapter errors. Use HTTP contract, createHandler, router, and a Standard Schema.',
		["@serve-tools/http-contract", "@serve-tools/router", "@standard-schema/spec"],
		[["@serve-tools/http-contract"], ["@serve-tools/http-contract/server"], ["@serve-tools/router"]],
		[
			["revision", "strict update and stale response"],
			["adapter", "typed request validation and methods"],
			["initial", "clone and last duplicate"],
		],
		[
			{
				id: "equal-wins",
				before: "input.revision <= current.revision",
				after: "input.revision < current.revision",
				requirementIds: ["revision"],
			},
			{
				id: "first-wins",
				before: "statuses.set(valid.value.service, { ...valid.value });",
				after: "if (!statuses.has(valid.value.service)) statuses.set(valid.value.service, { ...valid.value });",
				requirementIds: ["initial"],
			},
		],
	),
	task(
		"fixed-ping-client",
		"http-contract",
		"Fixed typed ping client",
		'createPingClient(fetchImpl, baseURL): { ping(init?): Promise<{ ready: boolean; requestURL: string }> }. fetchImpl must be callable and baseURL an absolute HTTP(S) URL string; reject invalid values with TypeError. Define an internal fixed GET /ping contract and use createStaticClient with the injected fetch and base URL. Resolve /ping from the origin root, so requestURL is always the configured new URL("/ping", baseURL).href and never Response.url. ping init, when supplied, must be a non-null object; pass its signal and non-accept headers through, while the client owns method GET and the JSON Accept header. For declared 200 JSON { ready: boolean }, resolve the exact fresh { ready: body.ready, requestURL }; for declared 503 JSON { error: "unavailable" }, resolve { ready: false, requestURL }. Malformed JSON, non-JSON content, invalid declared bodies, undeclared statuses, abort, and network failures must reject; preserve native abort/network failure identity. Use the static HTTP client.',
		["@serve-tools/http-contract"],
		[["@serve-tools/http-contract/client/static"]],
		[
			["request", "base URL resolution and request metadata"],
			["status", "200 and declared 503 result"],
			["errors", "protocol and native failures reject"],
		],
		[
			{
				id: "relative-for-other-base",
				before: 'new URL("/ping", base).href',
				after: 'new URL(base.pathname === "/root/" ? "/ping" : "ping", base).href',
				requirementIds: ["request"],
			},
			{
				id: "ready-503",
				before: "{ ready: false, requestURL } : { ready: result.body.ready, requestURL }",
				after: "{ ready: true, requestURL } : { ready: result.body.ready, requestURL }",
				requirementIds: ["status"],
			},
		],
	),
];
