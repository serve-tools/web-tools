{
	"name": "@serve-tools/signal",
	"version": "0.1.1",
	"description": "Implements the Stage 1 TC39 Signals proposal without global mutation",
	"keywords": [
		"computed",
		"dependency",
		"effect",
		"fine-grained",
		"memoization",
		"polyfill",
		"ponyfill",
		"proposal",
		"reactive",
		"reactivity",
		"scheduling",
		"signal",
		"state",
		"tc39",
		"watcher"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/signals/signal#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "signals/signal"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/signal.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"benchmark": "vitest run --config vitest.benchmark.config.ts",
		"build": "npm run clean && tsc --build --force tsconfig.json",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run test:node && npm run test:browser",
		"test:browser": "vitest run --config vitest.browser.config.ts",
		"test:node": "vitest run --config vitest.config.ts",
		"typecheck": "tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	}
}
