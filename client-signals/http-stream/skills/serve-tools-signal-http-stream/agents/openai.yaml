interface:
  display_name: "Serve Tools Signal HTTP Stream"
  short_description: "Connect and observe HTTP stream Signals"
  default_prompt: "Use $serve-tools-signal-http-stream to connect this typed HTTP stream and observe its subscription as explicit Signal state."
