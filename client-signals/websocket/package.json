{
	"name": "@serve-tools/signal-websocket",
	"version": "0.1.0",
	"description": "Typed WebSocket subscriptions observed as explicit Signal state",
	"keywords": [
		"reactive",
		"signal",
		"subscription",
		"typescript",
		"websocket"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client-signals/websocket#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client-signals/websocket"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/signal-websocket.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"benchmark": "npm run build:dependencies && vitest run --config vitest.benchmark.config.ts",
		"build": "npm run clean && tsc --build --force tsconfig.json",
		"build:dependencies": "npm run build --workspace @serve-tools/client-websocket --workspace @serve-tools/signal-messaging",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && npm run test:node && npm run test:browser",
		"test:browser": "vitest run --config vitest.browser.config.ts",
		"test:node": "vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-websocket": "^0.1",
		"@serve-tools/signal-messaging": "^0.2"
	}
}
