{
	"name": "@serve-tools/signal-dom-demo",
	"version": "0.0.0",
	"private": true,
	"description": "Browser demo for @serve-tools/signal-dom",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && npm run build:bundle",
		"build:bundle": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "node scripts/build-local-dependency.mjs && npm run typecheck:local",
		"typecheck:local": "tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/signal": "^0.1.3",
		"@serve-tools/signal-dom": "^0.3.0"
	},
	"devDependencies": {
		"typescript": "7.1.0-0 - 7.1.0",
		"vite": "8.2.2"
	},
	"engines": {
		"node": ">=22"
	}
}
