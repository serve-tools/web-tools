{
	"name": "@serve-tools/signal-shared-http-stream",
	"version": "0.1.0",
	"description": "SharedWorker HTTP stream clients with subscription Signal state",
	"keywords": [
		"http",
		"reactive",
		"sharedworker",
		"signal",
		"subscription"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client-signals/shared-http-stream#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client-signals/shared-http-stream"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/signal-shared-http-stream.js",
		"./scope/*": "./dist/scope/*.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"build": "tsc --build --force tsconfig.json",
		"build:dependencies": "npm run build:dependencies --workspace @serve-tools/client-shared-http-stream --workspace @serve-tools/signal-http-stream && npm run build --workspace @serve-tools/client-shared-http-stream --workspace @serve-tools/signal-http-stream",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && npm run build && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-shared-http-stream": "^0.1",
		"@serve-tools/signal-http-stream": "^0.1"
	}
}
