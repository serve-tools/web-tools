interface:
  display_name: "Serve Tools Signal Shared HTTP Stream"
  short_description: "Serve and observe shared HTTP Signals"
  default_prompt: "Use $serve-tools-signal-shared-http-stream to serve and connect this shared HTTP stream and observe its subscription as Signal state."
