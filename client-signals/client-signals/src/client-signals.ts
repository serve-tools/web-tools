/** Signal-backed IndexedDB operations and reactive queries. */
export * as db from "./lib/db.js";

/** Signal-aware DOM, stylesheet, and lifecycle helpers. */
export * as dom from "./lib/dom.js";

/** Signal-backed EventSource connections and latest-event observations. */
export * as eventSource from "./lib/event-source.js";

/** Signal-backed EventTarget state and media-query observations. */
export * as eventTarget from "./lib/event-target.js";

/** Signal-backed HTTP streaming clients and subscription observations. */
export * as httpStream from "./lib/http-stream.js";

/** Signal-backed messaging clients, servers, and subscription observations. */
export * as messaging from "./lib/messaging.js";

/** Signal-backed shared IndexedDB clients and reactive queries. */
export * as sharedDb from "./lib/shared-db.js";

/** Signal-backed shared EventSource clients and latest-event observations. */
export * as sharedEventSource from "./lib/shared-event-source.js";

/** Signal-backed shared HTTP streaming clients and subscription observations. */
export * as sharedHttpStream from "./lib/shared-http-stream.js";

/** Signal-backed shared WebSocket clients and subscription observations. */
export * as sharedWebsocket from "./lib/shared-websocket.js";

/** Signal-backed shared WebTransport clients and subscription observations. */
export * as sharedWebtransport from "./lib/shared-webtransport.js";

/** Signal-backed Web Storage clients and reactive key watches. */
export * as storage from "./lib/storage.js";

/** Signal-backed WebSocket clients and subscription observations. */
export * as websocket from "./lib/websocket.js";

/** Signal-backed WebTransport clients and subscription observations. */
export * as webtransport from "./lib/webtransport.js";
