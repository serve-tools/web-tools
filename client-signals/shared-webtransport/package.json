{
	"name": "@serve-tools/signal-shared-webtransport",
	"version": "0.2.0",
	"description": "SharedWorker WebTransport clients with subscription Signal state",
	"keywords": [
		"reactive",
		"sharedworker",
		"signal",
		"subscription",
		"webtransport"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client-signals/shared-webtransport#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client-signals/shared-webtransport"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/signal-shared-webtransport.js",
		"./scope/*": "./dist/scope/*.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"build": "tsc --build tsconfig.json",
		"build:dependencies": "npm run build:dependencies --workspace @serve-tools/client-shared-webtransport --workspace @serve-tools/signal-messaging && npm run build --workspace @serve-tools/client-shared-webtransport --workspace @serve-tools/signal-messaging",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && npm run build && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-shared-webtransport": "^0.1",
		"@serve-tools/signal-messaging": "^0.3"
	}
}
