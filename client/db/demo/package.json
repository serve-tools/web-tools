{
	"name": "@serve-tools/client-db-demo",
	"version": "0.0.0",
	"private": true,
	"description": "Browser demos for @serve-tools/client-db",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "npm run build --workspace @serve-tools/client-db && tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-db": "*"
	}
}
