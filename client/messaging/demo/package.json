{
	"name": "@serve-tools/client-messaging-demo",
	"version": "0.0.0",
	"private": true,
	"description": "SharedWorker demos for @serve-tools/client-messaging",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && npm run build:bundle",
		"build:bundle": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "node scripts/build-local-dependency.mjs && npm run typecheck:local",
		"typecheck:local": "tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-messaging": "^0.2"
	},
	"devDependencies": {
		"typescript": "7.0.2",
		"vite": "8.2.1"
	},
	"engines": {
		"node": ">=22"
	}
}
