{
	"name": "@serve-tools/client-messaging-demo",
	"version": "0.0.0",
	"private": true,
	"description": "SharedWorker demos for @serve-tools/client-messaging",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "npm run build --workspace @serve-tools/client-messaging && tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-messaging": "*"
	}
}
