{
	"name": "@serve-tools/client-webtransport",
	"version": "0.1.0",
	"description": "Typed requests, subscriptions, and datagrams over WebTransport",
	"keywords": [
		"browser",
		"datagram",
		"quic",
		"realtime",
		"rpc",
		"subscription",
		"webtransport"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client/webtransport#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client/webtransport"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/client-webtransport.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"benchmark": "npm run build:dependencies && vitest run --config vitest.benchmark.config.ts",
		"build": "tsc --build tsconfig.json",
		"build:dependencies": "npm run build --workspace @serve-tools/realtime-protocol --workspace @serve-tools/client-realtime",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && npm run test:node && npm run test:browser",
		"test:browser": "vitest run --config vitest.browser.config.ts",
		"test:node": "vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-realtime": "^0.1",
		"@serve-tools/realtime-protocol": "^0.1"
	}
}
