{
	"name": "@serve-tools/client-shared-http-stream",
	"version": "0.1.0",
	"description": "SharedWorker-coordinated typed HTTP requests and streaming subscriptions",
	"keywords": [
		"browser",
		"fetch",
		"http",
		"sharedworker",
		"subscription",
		"typescript"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client/shared-http-stream#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client/shared-http-stream"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/client-shared-http-stream.js",
		"./scope/*": "./dist/lib/scope/*.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"build": "tsc --build --force tsconfig.json",
		"build:dependencies": "npm run build:dependencies --workspace @serve-tools/client-http-stream && npm run build --workspace @serve-tools/client-http-stream --workspace @serve-tools/client-messaging",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run build:dependencies && vitest run --config vitest.config.ts",
		"typecheck": "npm run build:dependencies && npm run build && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-http-stream": "^0.1",
		"@serve-tools/client-messaging": "^0.2"
	}
}
