{
	"name": "@serve-tools/client-shared-db-demo",
	"version": "0.0.0",
	"private": true,
	"description": "SharedWorker demo for @serve-tools/client-shared-db",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "node scripts/build-local-dependency.mjs && tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-db": "^0.1",
		"@serve-tools/client-shared-db": "^0.2"
	},
	"devDependencies": {
		"typescript": "7.0.2",
		"vite": "8.2.1"
	},
	"engines": {
		"node": ">=22"
	}
}
