{
	"name": "@serve-tools/client-shared-db",
	"version": "0.2.1",
	"description": "SharedWorker-coordinated IndexedDB operations and change subscriptions",
	"keywords": [
		"browser",
		"cross-tab",
		"database",
		"idb",
		"indexeddb",
		"messageport",
		"offline-first",
		"shared-worker",
		"sharedworker",
		"subscription",
		"typescript",
		"web-api",
		"web-platform"
	],
	"homepage": "https://github.com/serve-tools/web-tools/tree/main/client/shared-db#readme",
	"bugs": {
		"url": "https://github.com/serve-tools/web-tools/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/serve-tools/web-tools.git",
		"directory": "client/shared-db"
	},
	"license": "MIT-0",
	"author": "Jonathan Neal <jonathantneal@hotmail.com> (https://jonneal.dev)",
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/client-shared-db.js",
		"./scope/shared-worker": "./dist/lib/scope/shared-worker.js",
		"./scope/window": "./dist/lib/scope/window.js"
	},
	"files": [
		"dist",
		"skills"
	],
	"scripts": {
		"benchmark": "npm run build:dependencies && vitest run --config vitest.benchmark.config.ts",
		"build": "tsc --build tsconfig.json",
		"build:dependencies": "npm run build --workspace @serve-tools/client-db && npm run build --workspace @serve-tools/client-messaging",
		"check:package": "publint && attw --pack . --profile esm-only",
		"clean": "rm -rf dist",
		"test": "npm run test:browser",
		"test:browser": "npm run build:dependencies && vitest run --config vitest.browser.config.ts",
		"typecheck": "npm run build:dependencies && tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json"
	},
	"dependencies": {
		"@serve-tools/client-db": "^0.1.3",
		"@serve-tools/client-messaging": "^0.2.2"
	}
}
