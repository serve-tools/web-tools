interface:
  display_name: "Serve Tools Client HTTP Stream"
  short_description: "Build typed binary HTTP stream clients"
  default_prompt: "Use $serve-tools-client-http-stream to implement this typed binary HTTP request and streaming subscription client with authentication and cancellation."
