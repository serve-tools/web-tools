{
	"name": "@serve-tools/client-storage-demo",
	"version": "0.0.0",
	"private": true,
	"description": "Browser demo for @serve-tools/client-storage",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "npm run build --workspace @serve-tools/client-storage && tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-storage": "*"
	}
}
