{
	"name": "@serve-tools/client-keyboard-demo",
	"version": "0.0.0",
	"private": true,
	"description": "Browser demo for @serve-tools/client-keyboard",
	"type": "module",
	"scripts": {
		"build": "npm run typecheck && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"typecheck": "node scripts/build-local-dependency.mjs && tsc --project tsconfig.json"
	},
	"dependencies": {
		"@serve-tools/client-keyboard": "^0.1"
	},
	"devDependencies": {
		"typescript": "7.0.2",
		"vite": "8.2.1"
	},
	"engines": {
		"node": ">=22"
	}
}
